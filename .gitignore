# Rust build output
target/

# dependencies (TS SDK lives under sdk/ after the Phase 0 restructure)
node_modules/
sdk/node_modules/

# build output
dist/
sdk/dist/

# cloned reference repos — study only, NOT part of loopd, do not commit
reference/

# loopd runtime data
.loopd/

# docs & planning — kept on disk, intentionally untracked.
# Blanket-ignore every markdown file so planning notes never reach the repo;
# the named docs and the claude/ folder are listed too for clarity.
*.md
PLAN.md
REFERENCES.md
ARCHITECTURE.md
HANDOFF.md
claude/

# logs / os / editor
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
