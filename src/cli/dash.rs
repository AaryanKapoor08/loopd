//! `loop dash` — the live TUI cockpit. Stub until Phase 5 (ratatui).

use anyhow::Result;

pub fn dash() -> Result<()> {
    println!("loop dash: the live TUI cockpit lands in Phase 5. For now use `loop ps`.");
    Ok(())
}
